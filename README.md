# Using Tilt (tilt.dev) to build up dev environment.

In order to run Tilt, these are prerequisites:

1. Docker, to build containers
2. kubectl, in order to get information about your cluster
3. A local Kubernetes cluster (It's a feature from Docker Desktop, which available for MacOS and Windows)

More details can be found here: https://docs.tilt.dev/install.html
